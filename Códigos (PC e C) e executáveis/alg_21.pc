inteiro principal () {
    inteiro j;
    real soma = 0, pii;
    para (j=1 ; 1000 ; 1) {
        soma = soma + 1/pow(j,2);
        pii = raizquadrada (6*soma);
        escreva ("%real\n", pii);
    }
    escreva ("\n");
}