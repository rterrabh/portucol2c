inteiro fatrecursivo (inteiro n) {
    se (n==0)
        retorne 1;
    senao se (n==1)
        retorne 1;

    senao
    retorne n * fatrecursivo(n-1);
}

inteiro principal () {
    inteiro n;
    escreva ("Digite um numero ");
    leia ("%inteiro", n);
    escreva ("%inteiro", fatrecursivo(n));
}