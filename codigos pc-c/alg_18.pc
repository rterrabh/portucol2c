inteiro principal () {
    texto s, c;
    inteiro i, x;
    escreva ("Digite um nome : ");
    leia ("%texto", s);
    x = strlen(s);
    inteiro j = 0;
    para (i=x-1 ; 0 ; -1) {
        c[j] = s[i];
        j++;
    }
    c[j] = '\0';
    escreva ("%texto", c);
}
